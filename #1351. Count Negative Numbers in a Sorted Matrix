def countNegatives(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        count = 0
        for i in grid:
            count += len([j for j in i if j<0])
            
        return(count)
